{
  "private": true,
  "scripts": {
    "build": "elm-make --yes src/Game.elm --output=./elm.js && cp index.template.html index.html",
    "server": "elm-reactor"
  },
  "dependencies": {
    "elm": "^0.17.0"
  }
}
